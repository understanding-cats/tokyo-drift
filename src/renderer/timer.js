/**
 * Starts the timer and create the session accordingly
 * (UI changes are also handle there)
 * This function should fail if there is already a session with the same name
 * @param {string} name - Name of the session
 * @returns {boolean} - True if success else False
 */
function startTimer(name) {
  //
}

/**
 * Pause the timer
 * If there is no timer the function should fail
 * @returns {boolean} - True if success else False
 */
function pauseTimer() {
  //
}

/**
 * Unpause the timer
 * If there is no timer the function should fail
 * @returns {boolean} - True if success else False
 */
function unpauseTimer() {
  //
}

/**
 * Cancel the timer
 * If there is no timer the function should fail
 * @returns {boolean} - True if success else False
 */
function cancelTimer() {
  //
}
